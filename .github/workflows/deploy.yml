name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
      
      - name: Test - check UNINASSAU in index.html
        run: grep -q "UNINASSAU" src/pages/index.html && echo "ok" || (echo "UNINASSAU not found in index.astro" && exit 1)

    #   - name: Deploy to Cloudflare Pages
    #     uses: cloudflare/pages-action@v1
    #     with:
    #       apiToken: ${{ secrets.CF_API_TOKEN }}
    #       accountId: ${{ secrets.CF_ACCOUNT_ID }}
    #       projectName: ${{ secrets.CF_PROJECT_NAME }}
    #       directory: dist
    #       gitHubToken: ${{ secrets.GITHUB_TOKEN }}